---
title: 快速开始
description: 创建并运行第一个 AI Workflow 工作流。
---

# 快速开始

本节将带你创建一个最简单的工作流，并完成首次测试运行。

## 1. 创建应用

进入工作室，点击新建应用并填写应用名称。创建完成后会自动进入工作流编辑器。

## 2. 添加节点

在画布中添加需要的节点，并按照业务流程连接它们：

1. 使用 Start 节点声明工作流输入。
2. 添加 LLM、HTTP 或 Code 等处理节点。
3. 使用 End 节点整理最终输出。

## 3. 测试运行

点击顶部的“测试运行”，填写 Start 节点要求的输入，然后查看每个节点的执行结果与追踪信息。

```ts
const input = 'Hello, workflow!'

return { message: input }
```

> 测试通过后再发布工作流，外部 API 才会使用最新发布版本。
